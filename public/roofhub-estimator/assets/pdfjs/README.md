PDF.js is installed locally by `npm install` and copied here by the postinstall
script. The ZIP intentionally does not bundle node_modules, PDF.js binaries or
font assets. No CDN or third-party PDF upload is used. PNG/JPEG/WebP takeoff and
manual measurement work without this optional runtime module.
